import type { ComponentProps, FC, PropsWithChildren, ReactNode } from 'react';
/**
 * Props for the TimelinePoint component.
 * @type TimelinePointProps
 * @extends {PropsWithChildren<ComponentProps<'div'>>}
 */
export type TimelinePointProps = PropsWithChildren<ComponentProps<'div'> & {
    /**
     * The icon to be displayed in the TimelinePoint.
     * @type {ReactNode}
     * @default ''
     */
    icon?: ReactNode;
    /**
     * Additional class name for the TimelinePoint.
     * @type {string}
     * @default ''
     */
    className?: string;
}>;
export declare const gradientColor: string[];
export interface TimelinePointStyle {
    root: {
        horizontal: {
            on: string;
            off: string;
        };
        icon: {
            on: {
                base: string;
                inner: string;
            };
            off: {
                base: string;
            };
        };
    };
}
export declare const TimelinePoint: FC<TimelinePointProps>;
