import type { FC, PropsWithChildren } from 'react';
import { TimelineBodyStyle } from './TimelineBody';
import { TimelineContentStyle } from './TimelineContent';
import { TimelineItemStyle } from './TimelineItem';
import { TimelinePointStyle } from './TimelinePoint';
import { TimelineTimeStyle } from './TimelineTime';
import { TimelineTitleStyle } from './TimelineTitle';
import { KeepColors, KeepSizes } from '../../Keep/KeepTheme';
export interface TimelinePointSizes extends Pick<KeepSizes, 'sm' | 'md' | 'lg' | 'xl'> {
    [key: string]: string;
}
export interface TimelinePointColor extends Pick<KeepColors, 'info' | 'error' | 'success' | 'warning' | 'gray'> {
    [key: string]: string;
}
/**
 * Props for the Timeline component.
 * @type {TimelineProps}
 */
export type TimelineProps = PropsWithChildren<{
    /**
     * Additional class name for the Timeline component.
     * @type {string}
     * @default ''
     */
    className?: string;
    /**
     * Determines if the timeline should be displayed horizontally.
     * @type {boolean}
     * @default false
     */
    horizontal?: boolean;
    /**
     * Determines if the timeline should have a gradient point.
     * @type {boolean}
     * @default false
     */
    gradientPoint?: boolean;
    /**
     * The color of the gradient.
     * @type {keyof TimelinePointColor}
     * @default 'info'
     */
    gradientColor?: string;
    /**
     * The type of the timeline bar.
     * - 'solid': Solid line.
     * - 'dashed': Dashed line.
     * @type {'solid' | 'dashed'}
     * @default 'solid'
     */
    timelineBarType?: 'solid' | 'dashed';
}>;
export interface keepTimelineTheme {
    root: TimelineComponentStyle;
    body: TimelineBodyStyle;
    content: TimelineContentStyle;
    item: TimelineItemStyle;
    point: TimelinePointStyle;
    time: TimelineTimeStyle;
    title: TimelineTitleStyle;
}
interface TimelineComponentStyle {
    horizontal: {
        on: string;
        off: string;
    };
    barType: {
        solid: string;
        dashed: string;
    };
}
export declare const Timeline: FC<TimelineProps> & {
    Item: FC<import("./TimelineItem").TimelineItemProps>;
    Point: FC<import("./TimelinePoint").TimelinePointProps>;
    Content: FC<import("./TimelineContent").TimelineContentProps>;
    Time: FC<import("./TimelineTime").TimelineTimeProps>;
    Title: FC<import("./TimelineTitle").TimelineTitleProps>;
    Body: FC<import("./TimelineBody").TimelineBodyProps>;
};
export {};
