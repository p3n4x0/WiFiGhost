import { FC, ReactNode } from 'react';
export interface PopoverTitleProps {
    children?: ReactNode;
    className?: string;
    [key: string]: any;
}
export declare const Title: FC<PopoverTitleProps>;
